import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
  container: {
    alignItems: "center",
    marginTop: 200,
  },
  title: {
    fontSize: 40,
    fontWeight: "800",
    width: "80%",
    textAlign: "left",
    marginBottom: 80,
  },
});
